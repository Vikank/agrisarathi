import 'package:flutter/material.dart';

class SingleCropSuggestion extends StatelessWidget {
  const SingleCropSuggestion({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
